{
  "name": "Multi_Agent_Enterprise_LLM_Orchiestrator-1788529826197",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "@cap-js/hana": "^3",
    "@sap/cds": "^10",
    "@sap/xssec": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^3"
  },
  "scripts": {
    "start": "cds-serve",
    "metadata": "node scripts/fetch-metadata.mjs",
    "tools:generate": "node scripts/generate-tools.mjs",
    "tools:preview": "node scripts/post-tools.mjs --dry-run",
    "tools:post": "node scripts/post-tools.mjs"
  },
  "private": true,
  "cds": {
    "requires": {
      "[production]": {
        "auth": "xsuaa"
      }
    }
  }
}
